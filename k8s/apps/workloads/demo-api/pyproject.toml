[project]
name = "homelab-garden-demo-api"
version = "0.1.0"
description = "Tiny stdlib demo API for homelab-garden observability exercises."
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = ["pytest>=8"]

[tool.uv]
package = false

