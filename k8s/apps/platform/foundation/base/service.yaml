apiVersion: v1
kind: Service
metadata:
  name: platform-smoke
  namespace: platform
  labels:
    app.kubernetes.io/name: platform-smoke
    app.kubernetes.io/part-of: homelab-garden
    homelab-garden.io/layer: platform
spec:
  selector:
    app.kubernetes.io/name: platform-smoke
  ports:
    - name: http
      port: 80
      targetPort: http
