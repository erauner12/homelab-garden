apiVersion: apps/v1
kind: Deployment
metadata:
  name: wrong-layer
  labels:
    app.kubernetes.io/name: wrong-layer
    app.kubernetes.io/part-of: homelab-garden
    homelab-garden.io/layer: platform
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: wrong-layer
  template:
    metadata:
      labels:
        app.kubernetes.io/name: wrong-layer
        app.kubernetes.io/part-of: homelab-garden
        homelab-garden.io/layer: platform
    spec:
      containers:
        - name: app
          image: example.invalid/demo:latest
          resources:
            requests:
              cpu: 10m
              memory: 16Mi
            limits:
              cpu: 50m
              memory: 64Mi
