apiVersion: apps/v1
kind: Deployment
metadata:
  name: selector-target
  labels:
    app.kubernetes.io/name: selector-target
    app.kubernetes.io/part-of: homelab-garden
    homelab-garden.io/layer: app
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: selector-target
  template:
    metadata:
      labels:
        app.kubernetes.io/name: selector-target
        app.kubernetes.io/part-of: homelab-garden
        homelab-garden.io/layer: app
    spec:
      containers:
        - name: app
          image: example.invalid/demo:latest
          resources:
            requests:
              cpu: 10m
              memory: 16Mi
            limits:
              cpu: 50m
              memory: 64Mi
