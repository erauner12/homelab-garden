apiVersion: v1
kind: Service
metadata:
  name: unmatched-selector
  labels:
    app.kubernetes.io/name: unmatched-selector
    app.kubernetes.io/part-of: homelab-garden
    homelab-garden.io/layer: app
spec:
  selector:
    app.kubernetes.io/name: does-not-exist
  ports:
    - name: http
      port: 80
      targetPort: 8080
