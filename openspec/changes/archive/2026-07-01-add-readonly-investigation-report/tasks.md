## 1. OpenSpec

- [x] 1.1 Create proposal, design, spec, and tasks for `add-readonly-investigation-report`.

## 2. Implementation

- [x] 2.1 Add a read-only investigation report script for `demo-api`.
- [x] 2.2 Add a Garden run action and `investigate-demo` workflow.
- [x] 2.3 Ensure generated investigation reports are not checked in.
- [x] 2.4 Document the optional investigation workflow.

## 3. Validation

- [x] 3.1 Run `bash -n` for the new shell script.
- [x] 3.2 Run `openspec validate add-readonly-investigation-report --strict`.
- [x] 3.3 Run `openspec validate --all --strict`.
- [x] 3.4 Run Garden config/workflow validation where feasible.
